%h1 Story

%p
  %b Title:
  = @story.title
%p
  %b Body:
  = @story.body

%br

%h2 Photos

%table
  %tr
    %th Image
    %th Title
    %th
    %th
    %th

  - @story.photos.each do |photo|
    %tr
      %td= link_to(image_tag(photo.image.thumb.url), [@story, photo]) if photo.image.thumb.url
      %td= photo.title
      %td= link_to 'show', [@story, photo]
      %td= link_to 'edit', edit_story_photo_path(@story, photo)
      %td= link_to 'del', [@story, @photo], :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'Edit', :edit_story
\|
= link_to 'Back', :stories
|
= link_to 'Add photo', new_story_photo_path(@story)

%br
%br

/ This division is replaced with a flash button by Uploadify
#photo_upload

/ Only load uploadify javascript on pages they are needed
= javascript_include_tag '/uploadify/swfobject.js', '/uploadify/jquery.uploadify.v2.1.0.min.js'

/ See application_helper.rb for this code
= photo_uploadify
